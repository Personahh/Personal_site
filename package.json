{
  "dependencies": {
    "express": "^4.18.1",
    "markdown-it": "^13.0.1",
    "pug": "^3.0.2"
  },
  "scripts": {
    "build": "node .",
    "serve": "yarn build && node serve",
    "dev": "nodemon --exec \"yarn serve\""
  },
  "nodemonConfig": {
    "ext": "js,json,pug",
    "ignore": [
      "out/*"
    ]
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}